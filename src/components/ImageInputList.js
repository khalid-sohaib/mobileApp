import { StyleSheet} from 'react-native'
import React from 'react'
import { HStack } from 'native-base'

export default function ImageInputList({imageUrls = []}) {
  return (
   <HStack>
        
   </HStack>
  )
}

const styles = StyleSheet.create({})